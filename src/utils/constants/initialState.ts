/**
 * @file Contains initial state object
 */
const initialState = {
  budget: {
    transactions: [],
    expense: 0,
    originalBalance: 0,
    currentBalance: 0,
  },
};

export default initialState;
